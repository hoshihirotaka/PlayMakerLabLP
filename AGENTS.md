## コンテンツ制作・マーケティングの指針

このセクションは、LP、バナー、チラシ、SNS投稿を作成する際の「魂」となるガイドラインです。

### 1. 核心となる思想 (Core Philosophy)
- **「プログラミング」ではなく「ゲームの作り方」を教える**: 
  文法ではなく「どうすれば面白くなるか」という企画・設計（プランニング）を主役に据える。
- **ショートカットの肯定**: 
  大人が20歳から始めた苦労を、今の子供はAIを使って10分で飛び越える。その「時間の解放」を価値とする。
- **目的主導型の学習**: 
  「このギミックを作りたいから学ぶ」という、遊びから逆算した学びを推奨する。

### 2. ターゲットペルソナ（解像度）
- **子供**: 
  小3でAIと壁打ちを始め、勝手に「企画会議」を始めてしまうような、好奇心の塊。Robloxという日常（遊び場）を自分の手で作り変えたいと願っている層。
- **保護者**: 
  35-44歳の共働き世帯、または教育熱心な層。子供に「消費」だけでなく「創造」の力を身につけさせたいと願っている。

### 3. 反応率を高めるフレームワーク (DECA)
制作物には以下の要素を同時多発的に含めること：
- **Discovery (発見)**: 「小3がAIとゲーム企画を始めた実話」など、常識を覆すエピソード。
- **Engage (関与)**: 「君がいつも遊んでいるRobloxを、君専用に改造しよう」。
- **Check (確認)**: 「Windows 5台・Mac 3台のプロ仕様環境」「現役プロ開発者による直接指導」。
- **Action (行動)**: 「まずは近所のワークショップへ（保育園から徒歩5分、地域密着）」。

### 4. 文章トーン＆マナー
- **熱量を可視化する**: 
  整いすぎた「塾のチラシ」ではなく、開発現場のワクワク感が伝わるライブ感のある言葉を使う。
- **比較対象**: 
  「ただのゲーム遊び」vs「世界へ届けるクリエイター」。10代でヒットを出す海外事例を引き合いに出し、日本からの挑戦を促す。
## SEO

---

## サイト構成

> ### ⚠️ 名前は「LP」だが、実態はホームページ（2026-08-23 追記）
>
> リポジトリ名が `PlayMakerLabLP` で、下の表も「体験会LP」と書いてあるため、
> **人もエージェントも「1ページで申し込ませる」前提で改修してしまいがち。**
> 運用の実態はすでにそこから離れている。
>
> **正しいのは、この下の「ツールの役割と購買ファネル」の表のほう。**
> LPの役割は **比較・検討**であって購入ではない。決済はDoorkeeperが担っている。
> 実際、いちばん成約している導線はDoorkeeperへ直行していて、このサイトを経由しない。
>
> **ここに来るのは「申し込もうとしている人」ではなく「調べている人」。**
>
> #### 「LPとして」改修すると起きること
>
> 1ページに全部載せる前提で直そうとするため、次のような無理が積み上がる。
>
> - **アンカーのズレ** … 7回修正して7回再発した。原因は縦に長すぎること
> - **`content-visibility` の導入** … 巨大な1ページを速くするための回避策で、
>   それ自体がアンカーのズレを生んで2026-08-22に削除した
> - **「日程へ直接飛ばす」広告URL** … 1ページ前提でしか出てこない発想
>
> **どれも「本来は複数ページであるべきものを1枚に押し込んでいる」ことの症状。**
>
> #### 改修するときの姿勢
>
> - 「どこに足すか」ではなく **「これは独立したページではないか」** を先に考える
> - ページを分ける案を、コストの高い最終手段ではなく **通常の選択肢**として扱う
> - 検討中の分割案は curriculum 側（PRIVATE）の残タスクに記録がある

| ページ | ファイル | 役割 | 固有のデータ・スクリプト |
|---|---|---|---|
| トップ | `index.html` | 教室紹介 → 体験 → **次回の開催（直近1件）** → 各要約 → 講師 → 想い → 作品 → 申込 | `events/*.js` / `curriculum/curriculum.js` |
| お子様向け日程 | `schedule.html` | **日程全件 ＋ 参加者の声 ＋ FAQ**。申し込む直前に必要なものを1ページに | `events/*.js` ／ **JSON-LD はここ** |
| 大人向け日程 | `adults.html` | 大人向けAI講座（条件・料金・日程・申込） | `events/*.js`（`audience:"adult"` の枠だけ） |
| プラン | `plans.html` | 体験会のあとの通い方（通常／個別） | — |
| カリキュラム | `curriculum.html` | 教材18件＋タブ | `curriculum/curriculum.js` |
| 作品ギャラリー | `works.html` | 作品＋ブラウザで遊べる作品 | — |
| 個別プラン | `online-home.html` | **限定公開のテスト中。`noindex` 維持・sitemap 除外** | — |

**ナビ（6ページ共通）**: カリキュラム / 作品ギャラリー / プラン / お子様向け日程 / 大人向け日程 / 講師

### 全ページに影響する場所（触るPRは単独で出す）

| ファイル | 影響範囲 |
|---|---|
| `style.css` | 見た目すべて |
| `js/site.js` | ナビ・計測・アンカー追従（**全6ページ**） |
| `js/render-events.js` | 日程の描画（index / schedule / adults の3ページ） |
| `js/render-curriculum.js` | カリキュラムの描画と件数（curriculum / index） |
| ナビの項目 | 6ページすべてに同じものを置く |
| `events/*.js` | index / schedule / adults の3ページ |

**日程の出し分けは `#event-list` の属性だけで行う**（スクリプトは1本を共有）。

```html
<div id="event-list" data-limit="1"></div>                 <!-- index：直近1件 -->
<div id="event-list"></div>                                 <!-- schedule：全件 -->
<div id="event-list" data-audience="adult"></div>           <!-- adults：大人向けの枠だけ -->
```

**判定はデータのプロパティで行い、ラベルの文言では判定しない。**
文言を書き換えたときに黙って壊れるため。

## ツールの役割と購買ファネル

| ツール | 役割 | ファネル位置 |
|--------|------|-------------|
| インスタ | 雰囲気・見込み客への訴求 | 認知 |
| note | 信頼構築・取り組みの深さ | 興味 |
| このサイト | 詳細説明（料金・コース・場所）。**調べている人が答えを探す場所** | 比較・検討 |
| Doorkeeper | 決済・今すぐ客の申込 | 購入 |

※ Threads・Xは未定（業界ネットワーク構築向きの位置づけ）

---

## 計測イベント（GA4）— 触る前に読む

サイトの操作は `js/site.js` の**デリゲート（1箇所でクリックを拾う形）**で計測している。
（以前は `index.html` 末尾に直接書いていたが、6ページに分けた際 `js/` へ切り出した）
個々のリンクに `onclick` を書かないのは、申込ボタンがJSで動的生成されるため。

| イベント | 対象 | 扱い |
|---|---|---|
| `doorkeeper_click` | Doorkeeperの申込リンク（`/events/` を含むもの） | **Google広告の主要コンバージョン** |
| `schedule_click` | 「日程を確認する」CTA 2箇所（`onclick` で送っている） | **サブアクション**（入札には使われない） |
| `community_click` | Doorkeeperトップ（コミュニティ登録） | 観測のみ |
| `nav_click` | ナビ・ロゴ・ハンバーガー・本文中のページ内リンク・受付準備中の固定CTA・`legal/` の4書面・「このあとの開催」の日付 | 観測のみ |
| `join_click` | 「今後の日程を受け取る」（`#join`） | 観測のみ |
| `curriculum_tab` | カリキュラムのタブ（すべて／Roblox／AI） | 観測のみ |

**どの項目を押したかは `click_item` パラメータで区別する。** イベント名を項目ごとに
分けない（GA4側の登録が増え、横断で見られなくなる）。

### 注意

- **入札に使っているのは `doorkeeper_click` だけ**（Google広告の「メインの操作」）。
  発火箇所を増やすと件数が水増しされ、スマート入札が壊れる。新しいボタンには別のイベント名を付ける
- **`schedule_click` は 2026-08-27 に「メインの操作」から「サブアクション」へ変更した。**
  サイト内移動なのに主要コンバージョンになっていて、`doorkeeper_click` と同一セッションで
  二重に計上されていた。**「コンバージョン」列にはメインの操作だけが入る。**
  数字を見るときは「すべてのコンバージョン」列と取り違えないこと（後者にはサブアクションも入る）
- **観測目的のイベントをキーイベントにしない。** Google広告へ取り込まれて入札に混ざる
- **`onclick` を持つCTAは `nav_click` の対象から外している**（二重計測になるため）。
  ページ内リンクに `onclick` で計測を足すときはこの前提を確認する
- **ハンバーガーの計測は開閉スクリプトより後に登録する必要がある**（`aria-expanded` を
  読んで開閉を判定しているため）。2つの `<script>` の前後を入れ替えない
- **サイト内の別ページへのリンクは、放っておくと計測から漏れる。** 同一ドメインなので
  拡張計測の離脱クリックが効かず、`#` リンクでもないため何も発火しない。**滞在時間は
  イベントに便乗して送られる**ので、押した人の時間が丸ごと消える。ページを増やすときは
  `nav_click` の⑤（相対パスの `.html` を拾う）で自動的に拾われるか確認する
- **コードに存在しないイベントはGA4側の設定で作られている。** GA4の設定変更で黙って
  止まることがあるので、重要なものはコードから明示的に送る
- 検証は `.claude/launch.json` の `lp` を起動し、**モバイル375px・touchイベント付き**で行う。
  `.click()` を直接呼ぶテストは `touchstart` を出さず、モバイル固有の不具合を素通りする

計測値・入札の実績は `~/curriculum/運営/LPアナリティクス/`（PRIVATE）。

---

## 情報の置き場（重要）

**このリポジトリはGitHub Pagesで公開しているためPUBLIC。** 事業情報を置かないこと。

| 種類 | 置き場 |
|---|---|
| 台本・原稿・キャプション本文・制作メモ・配布物 | **このリポジトリ** |
| 制作の方針・型（数値を伴わない指針） | **このリポジトリ**（[REEL-WORKFLOW.md](content/04_共通/REEL-WORKFLOW.md) など） |
| 広告のキーワード・入札・実績 | `~/curriculum/運営/LPアナリティクス/`（PRIVATE） |
| リールのインサイト数値・スクショ・分析 | `~/curriculum/運営/リールインサイト/`（PRIVATE） |
| チャネル横断の戦略メモ | **Notion**（ブロマス_ドキュメントハブ） |
| 売上・実績データ、同意書などの書面 | `~/curriculum`（PRIVATE）／`reference/` |

**計測値をこのリポジトリに書かない。** GitHub Pagesで公開しているためPUBLIC。
リーチ規模・視聴維持率・オーディエンス構成・CPC・検索語句は競合に見せたくない事業情報。

### ⚠️ 検証コマンドのバグで「問題なし」と誤報告した例（2026-08-25）

```bash
grep -c "ld+json" works.html    # → 0 が返る。だが JSON-LD は入っていた
```

**`+` は正規表現の量指定子。** `ld+json` は「ldjson」「lddjson」を探していて、
`application/ld+json` にはマッチしない。**これで「JSON-LDなし ✅」と報告し、
curriculum.html と works.html に Event の構造化データが残ったまま公開されていた**
（同じイベントを3ページが宣言している状態）。

正しくは次のどちらか。

```bash
grep -c "application/ld[+]json" works.html
grep -cF "application/ld+json" works.html
```

**検証コマンド自体が壊れていると、確認したつもりで通ってしまう。**
「0件だったのでOK」と書く前に、**わざと該当するファイルで試して1件出ることを確かめる。**

### 実際に破った例（2026-08-25 に発見）

ルールが書かれていても破られる。**「計画書だから」という理由で数値が混入した。**

`bromas_funnel_renewal_plan.md`（ルート直下）に、こう書かれていた。

> 実測では男性75〜84%、13-24歳が配信の40〜60%を占め、母親への到達は5本合計でも僅少だった

**オーディエンス構成そのもの。** 2026-08-21に追記され、4日間公開されていた。
発見時に `~/curriculum/運営/集客導線_統合改修プラン.md` へ移した。

**GitHubの履歴には残るので、入れてしまったら完全には消せない。書く前に止めるしかない。**

#### 気をつける形

**「方針」と「実測値」は同じ文書に書きたくなる。** 施策を決めるとき、根拠として
数字を引きたくなるのが自然だから。**だが根拠の数字はPRIVATE側に置き、こちらには
「〜という実測があったため」とだけ書く。**

新しくmdを足すときは、**数字が1つでも入るならPRIVATE側**に置くこと。

Notionの主なドキュメント: 「中高生向けコピー案とGoogle広告グループ設計」「Instagramリールの実績と分析」

## 使えるコネクタ

- **Canva** … デザインの読み取りと編集（テキスト差し替え・要素の移動/リサイズ/色）。ページ複製とフォント変更は不可
- **Notion** … ドキュメントハブへの読み書き
- **Roblox Studio（weppy-roblox-mcp）** … 教材プロジェクトの調査・スクリプト修正・プロパティ変更
- **Claude in Chrome** … note入稿など、ログイン済みセッションが要る作業
- ffmpeg は `/usr/local/bin/ffmpeg`（PATHから拾えないことがある）

## 制作ガイド

> **いまの状況と次にやることは [HANDOFF-2026-08-11.md](content/HANDOFF-2026-08-11.md) を先に読む。**
> 開催予定・進行中の施策・ツールの制約（noteの画像挿入不可、RobloxのUDim2設定不可など）がまとまっている。

| ガイド | 内容 |
|---|---|
| [SCHEDULE-PATTERNS.md](content/04_共通/SCHEDULE-PATTERNS.md) | タイムスケジュールのパターン集。曜日・会場ごとのコマ割りの型。**timetableを書く前にここを見る** |
| [NOTE-GUIDE.md](content/04_共通/NOTE-GUIDE.md) | note記事の書き方 |
| [NOTE-EDITOR-AUTOMATION.md](content/04_共通/NOTE-EDITOR-AUTOMATION.md) | noteエディタへの入稿手順 |
| [SNS-GUIDE.md](content/04_共通/SNS-GUIDE.md) | SNSシェア文言。Xの文字数の数え方・媒体別の使い分け |
| [REEL-WORKFLOW.md](content/04_共通/REEL-WORKFLOW.md) | リール制作。動画解析からSRT・キャプションまで |
| [content/README.md](content/README.md) | content配下のフォルダ定義と命名規則 |

## 当日資料（Canva）

講座の前後で使う「当日：ブロマス」（12ページ・Canva）がある。

> **スケジュールの差し替えは不要（2026-08-27）。** 当日にスケジュール確認そのものを
> 行わない方針になったため。**実際の進行で飛ばしてしまうから。**
> 以前は「毎回更新するのはスケジュールのみ」と書いていたが、その作業はなくなった。

- スケジュール … 更新しない（上記）。参照するなら `events/YYYY-MM-DD.js` の timetable が正
- カリキュラム紹介 … `curriculum/curriculum.json` の `status: published` をタグ別に掲載
- 趣旨 … LPの「ゲーム制作に特化した教室です」から起こしたもの。基本固定

## 作品ギャラリー（index.html #works）の方針

参加者の作品を載せるには、**回ごとに保護者の確認が必要**。利用規約 第5条による。

> 1. ワークショップ中に作成した成果物の著作権は、原則として制作者（参加者）に帰属します。
> 2. 当サービスは、教育・広報目的で成果物を紹介させていただく場合があります。その際は事前に保護者の方へご確認します。

- 撮影可否（同意書のチェックボックス）とは**別物**。写真がOKでも作品は都度確認が要る
- 2026-08-07時点: 体験会で同意書の運用が始まっていないため、**参加者の作品4点をコメントアウトで非公開**にしている（`index.html` の該当箇所にコメントで経緯あり）。画像は `assets/works/` に残してあるので、確認が取れた作品から個別に戻せる
- 非公開にしていないもの: ねこレストラン物語（身内・甥の作品）、TwinBlock 2048（講師の作品）
- **今後は確認が取れた作品に差し替えていく**。確認は親御様用Googleフォームの「作品の掲載可否」（Q8・必須）で記録する。**2026-08-07に設問追加済み、最初の収集機会は8/9**（[アンケート-子ども向けRoblox.md](content/01_開催前作業/アンケート/アンケート-子ども向けRoblox.md)）
- 穴埋めには**講師が作った教材の作例**を使う。同意の問題がなく、「何が作れるようになるか」を示せてカリキュラムへの導線にもなる

## 通い方のページ（plans.html）の方針

> **2026-08-25 追記:** ここは以前「教室ページ（school/）の方針」だった。
> `school/index.html` は 2026-05-17 の `add-school-page` ブランチで作られたが
> **一度も公開されないまま3か月放置され、2026-08-25 にブランチごと削除した。**
> 役割はいまの `plans.html`（通い方）が引き継いでいる。
> 削除したページの中身は `~/curriculum/運営/参考/`（PRIVATE）に残してある。

- 入会は無料体験会参加者のみ → CTAは常に体験会へ誘導する
- 「プログラミング教室」との差別化：特性から逆算・プロ直接指導・業界ネットワーク
- 料金は体験会後に個別案内（ページ上は枠のみ）
- コースは月の変わり目で選択式（Roblox・AI・アバター・企画など）

**⚠️ `online-home.html` の会場が「武蔵浦和駅から徒歩8分」なのは意図的。**
サイトの他の箇所は南与野で統一しているが、**個別プランは自宅開催で近所の方が対象**のため、
場所が分からないと申し込みの判断ができない。**表記ゆれとして直さないこと。**

**⚠️ `online-home.html`（個別プラン）へ公開リンクを張らないこと。**
限定公開のテスト中で `noindex` を維持している。案内は
「体験会当日にスタッフへお申し付けください／公式LINEにご招待」のままにする。

## イベント管理の仕様

### 仕組み

`index.html` にイベントをハードコードせず、イベントごとに独立したJSファイルで管理する。
ファイルを追加するだけでLPに反映される。

```
events/
  2026-06-26.js   ← イベントごとに1ファイル
  2026-XX-XX.js   ← 次回以降はここに追加
```

### イベント追加手順

**① `events/YYYY-MM-DD.js` を作成する**（既存ファイルをコピーして編集）

```js
(window.EVENTS = window.EVENTS || []).push({
  id: "2026-08-XX",           // ファイル名と合わせる（ソート順に使用）
  date: "2026年8月XX日（X）",
  datetime: "2026年8月XX日(X) HH:MM-HH:MM",
  location: "会場名",
  area: "エリア名",
  address: "〒XXX-XXXX 住所",
  access: "最寄駅: XX駅（徒歩X分）",
  target: "小学1年生〜高校生（保護者参加OK、小学生は同伴推奨）",
  capacity: "Robloxコース X名 / AIコース X名（予約優先）",
  equipment: "パソコン持参大歓迎（会場でも用意あり）",
  doorkeeperUrl: "https://gameschool.doorkeeper.jp/events/XXXXXX",
  note: "※注意事項があれば記載（省略可。開催日程の申込ボタン上に表示される）",
  timetable: [
    { time: "HH:MM-HH:MM", label: "Robloxコース", price: "X,XXX円" },
    { time: "HH:MM-HH:MM", label: "AIコース", price: "XXX円" }
  ]
});
```

**② 読み込む行を追加する（**合計4か所**）**

> ⚠️ **2026-08-25 に `schedule.html` を分けたため、2か所 → 4か所に増えた。**
> 日程を出すページが index.html と schedule.html の2つあり、それぞれに
> 「head の preload」と「body 下部の `<script>`」がある。

| ファイル | 場所 | 役割 |
|---|---|---|
| `index.html` | `<head>` の preload | 先読み |
| `index.html` | body下部の `<script>` | **直近1件**を出す（`data-limit="1"`） |
| `schedule.html` | `<head>` の preload | 先読み |
| `schedule.html` | body下部の `<script>` | **全件**を出す |

```html
<!-- ▼ イベント追加時はここに1行追加する（id昇順で並びます） -->
<script src="events/2026-XX-XX.js"></script>  ← 追加
```

```html
<link rel="preload" as="script" href="events/2026-XX-XX.js" />  ← 追加
```

**index.html も全件を読み込む。** 直近1件しか表示しないが、
「次回がどれになっても描ける」ようにするため。表示件数の出し分けは
`#event-list` の `data-limit` 属性だけで行っており、**描画スクリプトは
2ページで同じものを共有している**（片方だけ直すことはできない）。

preload を忘れると、そのイベントだけ先読みされず読み込みが遅れる
（表示は正しく出るので気づきにくい）。逆に **`<script>` から外したファイルの
preload を残すと**「先読みしたのに使われていない」という警告がコンソールに出る。
**両者は必ず一致させる。**

```bash
# 4か所すべての整合を検算する（何も出なければOK）
for f in index.html schedule.html; do
  echo "--- $f ---"
  diff <(grep -oE 'preload" as="script" href="events/[^"]*"' "$f" | sed 's/.*href="//;s/"//' | sort) \
       <(grep -oE '<script src="events/[^"]*"' "$f" | sed 's/.*src="//;s/"//' | sort)
done
# 2ページで読み込むイベントが一致しているか
# ※ コメント内の「過去開催: events/…」を拾わないよう <script src= に限定する
diff <(grep -oE '<script src="events/[^"]*"' index.html    | sed 's/.*src="//;s/"//' | sort) \
     <(grep -oE '<script src="events/[^"]*"' schedule.html | sed 's/.*src="//;s/"//' | sort)
```

**③ `schedule.html` の `<head>` 内 JSON-LD を更新する**（構造化データ・SEO用）

> ⚠️ **JSON-LD は index.html ではなく `schedule.html` にある**（2026-08-25 に移動）。
> 構造化データは、日程が実際に載っているページと一致させる必要があるため。

`@id` と各日時・URLをイベントに合わせて追記する。
**開催が終わった回は削除する**（終了した回が検索結果に出るため）。

### 日程だけ先に押さえた場合（comingSoon）

会場は取れたが時間・料金・Doorkeeperが未定のときは、`comingSoon: true` を付けて先に載せる。日程が合わなかった人に「次はいつか」を見せられる。

```js
  capacity: "調整中",
  comingSoon: true,          // 申込ボタンが「申込受付準備中」に変わる
  note: "日程のみ決まっています。時間・参加費・定員は調整中で、決まり次第このページでお知らせします。",
  timetable: [
    { time: "調整中", label: "タイムテーブルは決まり次第お知らせします" }
  ]
```

- **`timetable` は空配列にしない。** 描画側が必ず `map` するので、最低1行入れる
- 確定したら `comingSoon` を消し、`datetime` / `timetable` / `capacity` / `doorkeeperUrl` を埋める
- **JSON-LDには入れない。** 日時が確定してから追加する（曖昧な構造化データを出さない）
- 画面下の固定CTAは、次回開催が `comingSoon` のとき「開催予定・受付準備中」になり、リンク先が `#schedule` に切り替わる（Doorkeeper URLがないため）
- 開催曜日の傾向（日曜は日中／金曜は夜）は `note` に書く。日程セクション本体はいじらない

### 注意事項

- イベントは `id`（YYYY-MM-DD）の昇順で自動ソートされ、初期表示は「今日以降で最も近い開催日」が `open` 状態になる（すべて過去日の場合は最新日を `open`）
- `index.html` の変更は `<script>` 1行追加のみのため、ブランチ間のコンフリクトはほぼ発生しない
- ただし**日をまたぐブランチは、作業を再開する前に `git merge main` する**。コンフリクトが出なくても、分岐後にmainへ入ったページを消す差分になることがある（2026-08-12、規約類4書面が消える寸前だった）
- 各イベントファイルは独立しているため、編集しても他のイベントに影響しない
- ローカル確認はブラウザで `index.html` を直接開けばよい（サーバー不要）

---

## 教材リポジトリとの連携

カリキュラムの元ネタは別リポジトリ。ローカルではこのLPリポの兄弟ディレクトリに置く。

| | |
|--|--|
| ローカルパス | `../curriculum`（絶対パス例: `/Users/hirotakahoshi/curriculum`） |
| GitHub | https://github.com/hoshihirotaka/curriculum |

### 役割分担
- **正（source of truth）**: このLPの `curriculum/curriculum.json`（公開可否・公開名・表示タグ・集客文言はLPが決める）
- **教材側**: 事実の申告と提案のみ。`curriculum.json` は作らない

### やり取りファイル
| 方向 | ファイル | 置き場 |
|------|----------|--------|
| LP → 教材 | `curriculum/curriculum-feedback.md` | このリポ |
| 教材 → LP | `curriculum/curriculum-for-lp.md` | 教材リポ（`../curriculum/curriculum/`） |
| 教材エージェント用プロンプト | `curriculum/curriculum-agent-prompt.md` | このリポ（詳細は同ファイル） |

エージェントは必要に応じて `../curriculum` を直接読んでよい（コピペ渡しを省略してよい）。教材側の変更が必要なときはそのリポを作業ルートにする。
